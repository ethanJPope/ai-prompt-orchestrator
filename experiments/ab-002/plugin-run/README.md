# Daymark Visual Fixture

This repository contains the dependency-free Daymark marketing page and interactive weekly focus planner under `public/`.

Use Node.js 18 or newer. Run the local preview with `npm start` and use `npm test` for submitted verification.
