# v0.1 Verification Evidence

Date: 2026-08-03

Risk tier: V2 technical prototype.

## Verified

- The registry contains exactly 20 unique reviewer profiles.
- Minor follow-ups bypass the pipeline.
- A substantive task receives at least 10 passes and ends with the final arbiter.
- A high-risk OAuth task receives 12 reviewers, including configuration, authorization, and deployment coverage.
- A complete foundation audit receives all 20 profiles.
- The entitlement administration command changes an isolated account from active to inactive.
- The plugin's exact stdio MCP configuration starts the server successfully.
- The same MCP client succeeds while active and receives `subscription_inactive` immediately after deactivation.
- The reviewer registry and selection implementation are outside the plugin package.
- Common API-key and private-key patterns are absent from project files.
- The plugin manifest and bundled skill pass their system validators.
- The production dependency audit reports no known vulnerabilities.

## Commands

```powershell
pnpm install --frozen-lockfile
pnpm test
pnpm smoke
pnpm audit --prod
```

Plugin validation:

```powershell
py -3 <plugin-creator-skill>\scripts\validate_plugin.py <repo>\plugins\ai-prompt-orchestrator
```

Skill validation:

```powershell
py -3 <skill-creator-skill>\scripts\quick_validate.py <repo>\plugins\ai-prompt-orchestrator\skills\ai-prompt-orchestrator
```

## Results

- Automated tests: 8 passed, 0 failed.
- MCP smoke test: 12 reviewers while active; immediate `subscription_inactive` after deactivation.
- Dependency audit: no known vulnerabilities.
- Plugin validation: passed.
- Skill validation: passed.

## Unverified and intentionally absent

- OAuth 2.1 identity and token revocation.
- Hosted Streamable HTTP deployment.
- Real subscriptions, billing, refunds, or payment webhooks.
- Automatic Codex prompt-submit and stop hooks.
- Quality gains against ordinary Codex on real customer tasks.
- Unit economics and customer willingness to pay.
